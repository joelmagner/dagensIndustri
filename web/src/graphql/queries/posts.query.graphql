query Posts($url: String!, $limit: Int, $order: Boolean) {
  posts(url: $url, limit: $limit, order: $order) {
    title
    creator
    link
    content
    contentSnippet
    guid
    pubDate
    isoDate
    thumbnail {
      url
      type
      description
    }
  }
}
